# Dev: leave empty — Vite proxies /graphql → localhost:3000
# Prod / GitHub Pages: Railway Nest GraphQL (Actions secret VITE_GRAPHQL_URL)
#
#   VITE_GRAPHQL_URL=https://selfskills-api-production.up.railway.app/graphql
#
# Baked at build time (Vite).
VITE_GRAPHQL_URL=
