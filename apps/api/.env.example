# STEP 0 — скопируй в .env (не коммить .env)
# Локально (Postgres с хоста): localhost
# В Docker Compose (шаг 5): хост = db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/selfskills?schema=public
PORT=3000
