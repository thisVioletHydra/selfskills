# apps/api

NestJS + Apollo GraphQL + Prisma + Fastify.

```
GraphQL → resolver → service → Prisma → Postgres
```

Модули: `prisma`, `profile`, `skill`, `project`.

Запуск из корня: `pnpm back`. БД: `pnpm db up`.
