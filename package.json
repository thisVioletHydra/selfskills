{
  "name": "selfskills",
  "private": true,
  "type": "module",
  "scripts": {
    "front": "pnpm --filter web dev",
    "back": "pnpm --filter api start:dev",
    "db": "bash infra/db.sh",
    "build": "pnpm --filter api exec prisma generate && pnpm --filter api build && pnpm --filter web build"
  },
  "engines": {
    "node": "24"
  },
  "packageManager": "pnpm@11.22.0",
  "devDependencies": {
    "@selfskills/oxlint-rules": "workspace:*",
    "oxlint": "^1.79.0",
    "oxlint-tsgolint": "^7.0.2001"
  }
}
